import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

const dashboard = () => (
    <Card>
        <CardHeader title="Welcome to the lms administration" />
        <CardContent>Add, view,edit,delete profiles...</CardContent>
    </Card>
);

export default dashboard;